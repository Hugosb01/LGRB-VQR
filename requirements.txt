# =============================================================
# Dependencias del TFG — VQR para predicción de redshift de LGRBs
# =============================================================
# Probado con Python 3.10 / 3.11
# Instalar con:  pip install -r requirements.txt
# =============================================================

# --- Cálculo numérico y datos ---
numpy>=1.24,<2.0
pandas>=2.0
scipy>=1.11

# --- Machine learning clásico (preparación de datos + cross-validation) ---
scikit-learn>=1.3
statsmodels>=0.14        # RLM (Huber M-estimator) en prep_datos_TFG.ipynb
joblib>=1.3              # paralelización folds en Circuito_cuantico.py

# --- Visualización ---
matplotlib>=3.7
seaborn>=0.13

# --- Stack cuántico (Qiskit) ---
qiskit>=1.0
qiskit-machine-learning>=0.7
qiskit-algorithms>=0.3

# --- Jupyter (para los notebooks) ---
jupyter>=1.0
ipykernel>=6.0
notebook>=7.0
